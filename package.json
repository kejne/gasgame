{
  "name": "gasgame-grid-movement",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "tsc -b && vite build",
    "package": "npm run build",
    "test": "vitest run",
    "validate": "node scripts/validate.mjs",
    "visual:smoke": "node scripts/visual-smoke.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.0",
    "@vitejs/plugin-basic-ssl": "^2.0.0",
    "playwright-core": "^1.62.1",
    "typescript": "~5.8.3",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  }
}
